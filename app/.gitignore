# ── Dependências ─────────────────────────────────────────────
node_modules

# ── Next.js / build ──────────────────────────────────────────
.next
out
next-env.d.ts
*.tsbuildinfo

# ── Segredos / ambiente ──────────────────────────────────────
.env
.env.local
.env*.local
.env.example

# ── Testes (Vitest / Playwright) ─────────────────────────────
playwright-report
blob-report
test-results
.playwright
.playwright-mcp
screenshots

# ── Vercel ───────────────────────────────────────────────────
.vercel

# ── Kanban stray (o canônico é o .kanban/ da raiz) ───────────
.kanban

# ── Logs ─────────────────────────────────────────────────────
*.log
