# ── Dependências ─────────────────────────────────────────────
node_modules

# ── Next.js / build ──────────────────────────────────────────
.next
out
next-env.d.ts
*.tsbuildinfo

# ── PWA (Serwist gera estes no build) ────────────────────────
public/sw.js
public/sw.js.map
public/swe-worker-*.js

# ── Segredos / ambiente ──────────────────────────────────────
.env
.env.local
.env*.local
# Copias de seguranca do .env (ex.: .env.local.bak-asaas). Sem esta linha um
# backup de segredos fica visivel no `git status` esperando um `git add -A`.
.env.local.*
.env.example

# ── Testes (Vitest / Playwright) ─────────────────────────────
playwright-report
blob-report
test-results
.playwright
.playwright-mcp
screenshots

# ── Vercel ───────────────────────────────────────────────────
.vercel

# ── Kanban stray (o canônico é o .kanban/ da raiz) ───────────
.kanban

# ── Logs ─────────────────────────────────────────────────────
*.log
